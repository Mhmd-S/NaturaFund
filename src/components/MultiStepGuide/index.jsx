import React from 'react'

const index = ({ items }) => {

  const renderItems = () => {
    const items = Object.values(items).map((_, ind) => {}
  }

  return (
    <div>
      
    </div>
  )
}

export default index